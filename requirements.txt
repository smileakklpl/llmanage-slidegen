# 主管線的相依（repo root 執行的一切：verify_all.py、main.py、evalh/、tools/、tests/）。
#
# ingestion 後端的相依另外放在 src/backend/requirements.txt——那批有
# paddleocr / PyMuPDF / reportlab 等重量級套件，主管線一個都用不到，
# 混在一起只會讓 CI 白裝好幾百 MB。
# 兩份各自對應自己範圍內的程式碼，這是 .kiro/skills/update-requirements 的規定。
boto3>=1.34
botocore[crt]>=1.34    # `aws login` / SSO 憑證需要 CRT，否則連 STS 都打不通
openpyxl==3.1.5
pandas>=2.0
pydantic==2.13.4
pytest==9.1.1
python-pptx>=0.6
requests>=2.28

# 只有 tools/smoke_bedrock.py 需要（Mantle client）：
# pip install "anthropic[bedrock]"
