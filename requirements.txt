# 核心
pydantic>=2.0          # 資料契約與 schema 驗證（contracts/）
openpyxl>=3.1          # xlsx 讀寫（engine/）
python-pptx>=0.6       # 簡報生成（renderer/）

# LLM provider
requests>=2.28         # Ollama HTTP API
boto3>=1.34            # Bedrock（已在 us-east-1 實測通過，見 llm/bedrock.py）
botocore[crt]>=1.34    # `aws login` / SSO 憑證需要 CRT，否則連 STS 都打不通

# 只有 tools/smoke_bedrock.py 需要（Mantle client）：
# pip install "anthropic[bedrock]"

# 測試與分析
pytest>=7.0
pandas>=2.0            # 指標定義的獨立驗算（tests/）
