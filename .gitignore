# 環境目錄
.venv

# Python 快取
__pycache__/
*.py[cod]

# 比賽題目
source/*
!source/template.pptx

# 管線分階段除錯用的中間結果，可重生，不進版控
outputs/stages/

# MacOS file
.DS_Store

# 資料集
#
# 金管會月報是政府公開資料，其他模組的實作也要拿它當輸入，所以進版控
#（原始月報 + 轉檔產出，合計不到 1MB）。
# 命題素材（附件四）不進版控：那是主辦方提供的東西，這個 repo 是公開的。
#
# 注意寫法：這裡不能寫 `fixtures/data/` 再逐一 !例外——git 一旦排除掉
# 整個目錄就不會再走進去，底下的反向規則會全部失效。必須用 `/*` 排除
# 「目錄下的項目」，才有辦法逐一放行。
fixtures/data/*
!fixtures/data/fsc_114/
!fixtures/data/fsc_113_114/
!fixtures/data/金融業務資訊揭露/
# 與 fsc_114/ 同一份資料的單檔多表版型（tools/build_fsc_workbook.py 產出）。
# 它是 ingestion 兩種輸入版型的實測資料之一，README 與 Guide 都引用到，
# 不進版控的話別人 clone 下來會找不到檔案。
!fixtures/data/fsc_114_workbook.xlsx

# .ods 與 .xlsx 是同一份資料的兩種格式，程式只讀 xlsx
#（見 tools/ingest_fsc.py 的 _months()），.ods 純屬重複，不進版控
*.ods

# 生成結果：deck.pptx / deck_data.xlsx 是示範產出，刻意進版控供他人直接檢視；
# 分階段中間結果不進（規則在上面的 outputs/stages/）。

# Python
.pytest_cache/

# Frontend
node_modules/
src/frontend/.env
