[pytest]
pythonpath = .
testpaths = tests
addopts = -ra
timeout = 60
timeout_method = thread
markers =
    slow: requires real OCR model or large files
    integration: full API or multi-module integration test