# ingestion 後端的相依。主管線的相依在 repo root 的 requirements.txt。
#
# 安裝：在 src/backend/ 下 pip install -r requirements.txt
#
# OCR（paddleocr）不在這份清單裡，改放 requirements-ocr.txt。
# 理由是程式碼本來就把它當選用：visual_parser.py 在函式內才 import，
# 抓到 ImportError 會提示安裝指令。整套測試注入 FakeEngine，不碰真引擎，
# 所以 CI 與一般開發都不需要裝它——要跑真的 OCR 才裝。
beautifulsoup4==4.15.0
fastapi==0.140.4
openpyxl==3.1.5
pdfplumber==0.11.10
Pillow==12.3.0
pydantic==2.13.4
PyMuPDF==1.28.0
pypdf==6.14.2
pytest==9.1.1
pytest-timeout==2.4.0
python-multipart==0.0.32
reportlab==5.0.0
starlette==1.3.1

# Web UI 需要的額外相依（feat/web-ui）
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
uvicorn[standard]>=0.30.0
