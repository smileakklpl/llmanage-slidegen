# PPT 圖表原生性與資料同步設計

> **版本**：v0.3
> **狀態**：核心機制（PPT 圖表一致性）已 POC 驗證；資料輸入源已改為 backend ingestion 產出的 JSON；多 Agent 架構為設計中，逐步實作
> **對應規格書章節**：FR-2.2（圖表原生性）、FR-3（Excel 繪圖資料同步輸出）
> **v0.2 → v0.3 主要變更**：
> 1. 輸入源澄清：不再自行解析原始 Excel，改為消費 `src/backend` ingestion 管線輸出的 `UnifiedIngestionResult` **JSON**（含來源證據），原本的「分析後 Excel」中間產出物退化為選用的稽核落地格式
> 2. 模組路徑更正：簡報生成相關程式全數位於 `src/ppt_generation/`（原文件誤記為 `src/`）
> 3. 實作狀態更正：v0.2 文件將 `metric_store.py` / `chart_planner.py` / `verify_llm_chart_flow.py` 列為已完成，實際尚未撰寫，本版標示為待實作並定義其契約
> 4. 新增 LLM 憑證管理約定（`.venv/api_key/`）

---

## 1. 需求澄清

命題原文要求：

> 簡報中的圖表，**右鍵點擊「編輯資料」**，即可查看/修改原始資料表。

這**不是**「PPT 圖表連結到外部 Excel 檔案」，而是 PowerPoint 原生機制：圖表右鍵「編輯資料」開啟的，是**內嵌在 `.pptx` 檔案內部**的一份工作簿（embedded workbook），跟外部獨立的 `.xlsx` 檔案是兩個不同的東西。

因此正確的設計方向是：**生成 PPT 時，讓圖表的內嵌工作表與畫面顯示值同源自同一批解析後資料**，而非事後去「連結」一份外部 Excel。

---

## 2. 技術原理

### 2.1 PPT 圖表背後的三個部件

一張 PowerPoint 原生圖表，實際由三個部分組成：

| 部件 | 位置 | 作用 |
|------|------|------|
| Chart XML 快取值（`c:numCache` / `c:strCache`） | `.pptx` 內 `ppt/charts/chart1.xml` | PowerPoint **顯示**圖表時直接讀這裡，不會每次開檔都重新讀工作簿 |
| 內嵌工作簿（embedded workbook） | `.pptx` 內 `ppt/embeddings/Microsoft_Excel_Worksheet#.xlsx` | 使用者右鍵「編輯資料」時開啟、修改的來源 |
| 圖表物件本身（graphic frame） | slide XML 內 | 決定圖表在投影片上的位置、大小、類型 |

**風險點**：若這三者（尤其前兩者）數字不一致，就會出現「畫面上顯示 A 值，但右鍵編輯資料打開卻是 B 值」的問題——這正是附件三錯誤範例（P.5、P.7 座標軸與數值有誤）的技術根源之一。

### 2.2 解決方案：單一資料入口

python-pptx 的 `shapes.add_chart()` API，在**生成階段**會自動把同一批輸入資料，同時寫入 chart XML 快取與內嵌工作簿。只要全系統統一走這一個入口，兩者就保證一致，不需要額外的「同步」或「連結」邏輯。

```
ChartSpec（結構化資料）
        │
        ▼
   add_chart()  ←── 唯一入口，不手動操作底層 XML
        │
        ├──→ chart XML cache（畫面顯示值）
        └──→ 內嵌 workbook（右鍵「編輯資料」看到的值）

上述兩者由同一次 API 呼叫產生，天生一致。
```

**這個原則不因導入多 Agent 架構而改變**——無論是哪個 Agent 決定「要畫哪張圖」，最終落地生成圖表時，一律只能透過這個單一入口，Agent 本身沒有機會繞過去手動塞數字。

---

## 3. 整體資料流（v0.3）

v0.3 的關鍵調整：**簡報生成端不自己解析原始 Excel**。原始檔案的偵測、分類、抽取、品質驗證、來源證據標註，已由 `src/backend` 的 ingestion 管線完成，並輸出結構化 JSON。簡報生成端只消費這份 JSON。

```
┌──────────────────────────────────────────────────┐
│  Stage 0：原始數據檔案                              │
│  xlsx / csv / pdf / png / jpeg（附件四類多工作表報表）│
└────────────────────┬───────────────────────────┘
                     ▼
┌──────────────────────────────────────────────────┐
│  Stage 1：backend ingestion 管線（已實作）           │
│  src/backend/app/ingestion/pipeline.py             │
│  detector → security → classifier → extractor      │
│           → validator → normalizer                 │
│  輸出：UnifiedIngestionResult（JSON）                │
│  - datasets: UnifiedDatasetSpec[]                  │
│    · columns: key / label / data_type / unit       │
│    · records: 每格含 value + confidence + evidence  │
│    · evidence: 檔名 / 工作表 / 儲存格 / 頁碼 / bbox   │
│  - validation: 資料品質報告（pass / warning / fail）  │
│  - review_required_count: 需人工確認的資料集數        │
└────────────────────┬───────────────────────────┘
                     ▼
┌──────────────────────────────────────────────────┐
│  Stage 2：指標計算引擎（待實作）                      │
│  src/ppt_generation/data/metric_engine.py        │
│  - pandas，純確定性計算，零 LLM 參與                  │
│  - YoY / MoM、市占率、有效卡率、排名、趨勢外推          │
│  - 防呆：資料範圍外的指標不產出                        │
│    （僅單年資料 → 不計算 YoY；缺基期 → 不計算成長率）    │
│  - 每個指標保留 source_evidence，串回 Stage 1 的證據   │
└────────────────────┬───────────────────────────┘
                     ▼
┌──────────────────────────────────────────────────┐
│  Stage 3：MetricStore（待實作）                     │
│  src/ppt_generation/data/metric_store.py         │
│  系統唯一真相來源，兩個對外介面刻意分離：              │
│  - catalog_for_llm()：只給 metadata                 │
│    （metric_key / 名稱 / 單位 / 類別預覽），無實際數值  │
│  - get(metric_key)：查表取得實際數值（僅本地程式呼叫）  │
│  - computable_metric_keys()：可計算指標白名單          │
└────────────────────┬───────────────────────────┘
                     ▼
┌──────────────────────────────────────────────────┐
│  Stage 4：多 Agent 協作（詳見第 4 節）                │
│  ① 章節規劃 Agent → SectionPlan[]                    │
│  ② 圖表 Agent（含圖表 skill）→ ChartPlan[] → ChartSpec │
│  ③ 文字生成 Agent → PageNarrative（含 metric 佔位符）  │
│  ④ 審查 Agent → APPROVED / REJECTED(重試指定 Agent)   │
└────────────────────┬───────────────────────────┘
                     ▼
┌──────────────────────────────────────────────────┐
│  Stage 5：Renderer（待實作）                        │
│  src/ppt_generation/output/renderer.py           │
│  套用 source/template.pptx，                        │
│  圖表一律經 CHART_SKILLS[chart_type] → add_chart()   │
└────────────┬────────────────────────┬───────────┘
             ▼                        ▼
      chart XML cache          內嵌 workbook
     （畫面顯示值）            （右鍵編輯資料值）
             │                        │
             └───────────┬────────────┘
                         ▼
              同一入口生成，保證一致
                         │
        （另一條獨立路徑，同源自 MetricStore）
                         ▼
┌──────────────────────────────────────────────────┐
│  Stage 6：FR-3 外部稽核 .xlsx（待實作）              │
│  src/ppt_generation/output/excel_exporter.py     │
│  工作表命名：P.{頁碼}_{指標名稱}（對齊附件四）          │
│  每張工作表附來源追溯欄位（檔名／工作表／儲存格）       │
└────────────────────┬───────────────────────────┘
                     ▼
┌──────────────────────────────────────────────────┐
│  Stage 7：三方數值比對驗證（部分已實作）              │
│  src/ppt_generation/verification/                │
│      verify_chart_consistency.py                 │
│  chart XML cache ↔ 內嵌 workbook ↔ 外部 .xlsx       │
└──────────────────────────────────────────────────┘
```

### 3.1 為什麼由 backend JSON 作為輸入界面

1. **模組邊界清楚**：對齊 steering 的「模組間僅以 JSON 契約溝通」原則，簡報生成端不得直接 `openpyxl.load_workbook()` 讀原始檔案。
2. **來源追溯天生具備**：`UnifiedDataValue.evidence` 已帶檔名／工作表／儲存格／頁碼／OCR bbox，FR「任一簡報數字可回溯至來源儲存格」的需求不需要重新實作一次。
3. **多格式一次收斂**：xlsx、csv、pdf、圖片 OCR 都被 backend 收斂成同一個 `UnifiedDatasetSpec` 格式，簡報端只需處理一種資料形狀。
4. **切斷 LLM 與原始檔案的距離**：LLM 只讀 MetricStore 的 metadata，完全不觸碰原始 `source/附件四...xlsx`，也不觸碰 backend 的原始 records。
5. **人工確認狀態可傳遞**：`review_status` / `requires_human_review` 讓簡報端能拒絕使用未經確認的低信度 OCR 數據。

**重要原則不變**：LLM（無論哪個 Agent）只負責決策（畫哪種圖、寫什麼洞察、審查邏輯是否合理），**不參與任何數值計算**。所有數字必須來自 Stage 1 backend JSON → Stage 2 指標引擎 → Stage 3 MetricStore 這條確定性鏈路。

### 3.2 「分析後 Excel」在 v0.3 的定位變化

v0.2 把「分析後 Excel」設為 Stage 2 的必要中間產出物。v0.3 改為：MetricStore 直接由 backend JSON 加上指標計算在記憶體中建立，**不強制經過 Excel 落地**。落地的 `.xlsx` 只保留一個用途——Stage 6 的 FR-3 外部稽核輸出。理由是原本的設計會讓同一批數字多繞一次 Excel 讀寫，增加不必要的格式轉換誤差風險，而 Refresh 重放所需的持久化改由 DeckSpec JSON 負責。

---

## 4. 多 Agent 協作架構

### 4.1 設計理念

命題要求原本可以用單一 LLM 呼叫（prompt → 直接出圖表數據）處理，但那會導致 LLM 同時決策「畫什麼」與「數字是多少」，違反「LLM 不計算數字」的核心原則。因此拆成四個職責單一的 Agent，圖表決策與數值之間永遠隔著一層**查表動作**（MetricStore），LLM 沒有機會編造不存在的數字。

Orchestrator（確定性程式碼，非 LLM）負責在四個 Agent 間傳遞資料契約、觸發防呆檢查、控制中斷點（例如等待使用者確認章節），不交給 LLM 自行判斷流程走向。

### 4.2 四個 Agent 的資料契約

```
使用者描述 + (可選)章節清單
        │
        ▼
① 章節規劃 Agent (SectionPlannerAgent)
   input:  user_prompt, existing_sections?
   output: { status: "NEEDS_CONFIRMATION" | "READY",
             sections: SectionPlan[],
             question_to_user?: string }
   規則：若使用者未提供章節清單，必須回傳 NEEDS_CONFIRMATION，
         由 Orchestrator 中斷流程等待使用者輸入，不可自行假設。
        │ (使用者確認後，狀態變為 READY)
        ▼
② 圖表 Agent (ChartAgent) —— 本身是一個小型 Orchestrator，內部依序執行：
   a. LLM 呼叫：input  = SectionPlan + MetricStore.catalog_for_llm()（不含數值）
              output = ChartPlan[]（每項僅含 metric_key 引用，無數字欄位）
   b. 確定性檢查：chart_planner.validate_chart_plan()
      - metric_key 是否存在於 MetricStore 可計算指標白名單
      - chart_type 是否為已註冊 skill
      - series_names 是否存在於該 metric 下
   c. 確定性查表：chart_planner.resolve_chart_plan() → ChartSpec / ScatterSpec
   d. 依 chart_type 從 CHART_SKILLS registry 取出對應函式，實際呼叫產生圖表
        │
        ▼
③ 文字生成 Agent (NarrativeWriterAgent)
   input:  SectionPlan + 已 resolve 的 ChartSpec（可讀取實際數值，僅供「引用」）
   output: PageNarrative { text_with_placeholders, cited_metric_keys }
   限制：輸出文字中的數字只能以 {{metric_key}} 佔位符表示，
         禁止輸出字面數值；renderer 於最終組裝時才代入實際值。
        │
        ▼
④ 審查 Agent (ReviewerAgent) —— 分兩層檢查：
   數值層（確定性規則，不需 LLM）：
     - cited_metric_keys 是否都在 MetricStore 可計算指標白名單內
     - text_with_placeholders 是否含「裸數字」（正規表達式偵測）
   敘事/邏輯層（LLM 語意審查）：
     - 圖表類型是否適合資料形狀（比例資料誤用折線圖等）
     - 敘事方向是否與數值正負號矛盾
       （對應附件三 P.7「10.7% 高於 11.0%」的邏輯錯誤類型）
   output: APPROVED | REJECTED(reason, target_agent)
           REJECTED 時，Orchestrator 將任務退回 target_agent 重跑
```

### 4.3 圖表 Skill 註冊機制

圖表 Agent 不需要知道每種圖表的實作細節，只需要知道 skill 名稱字串。`chart_builder.py` 維護一個 registry：

```python
CHART_SKILLS = {
    "column": add_category_chart,   # 長條圖／排名圖／成長率圖
    "bar": add_category_chart,      # 橫條圖
    "pie": add_pie_chart,           # 市占率圖
    "scatter": add_scatter_chart,   # 規模 vs 成長散點圖
    "combo": add_combo_chart,       # 雙軸圖：長條（主軸）+ 折線（次軸）
    # "table" / "heatmap": 走原生表格（add_table），不透過 add_chart()，
    #            另外註冊在 table_builder.py 的 TABLE_SKILLS
}
```

新增圖表類型時，只需在此註冊一個函式，圖表 Agent 與 Orchestrator 端無需改動。

表格類 skill 註冊在 `table_builder.TABLE_SKILLS`，與圖表分開，因為兩者的驗收
標準不同：圖表要能右鍵「編輯資料」，表格不能（也不該假裝可以）。
`chart_planner.VISUAL_SKILLS` 是兩者的合集，供防呆白名單與 renderer 落版使用。

`chart_builder.py` 同時提供 `CHART_SKILL_TOOL_SCHEMAS`（給 LLM 的 tool schema）與 `dispatch_chart_skill()`（白名單查表 dispatcher）。tool schema 中**刻意沒有任何數值欄位**，LLM 只能填 `metric_key`；dispatcher 只從 registry 白名單取值，絕不 `eval`／`exec` LLM 輸出。

### 4.4 ChartPlan：LLM 唯一允許輸出的圖表決策格式

```python
@dataclass
class ChartPlan:
    slide_title: str
    chart_type: Literal["column", "bar", "pie", "line", "scatter"]
    chart_title: str
    metric_key: str          # 引用 MetricStore，不含實際數字
    series_names: list[str] | None = None
```

此結構**完全沒有數值欄位**，這是刻意設計，逼迫 LLM 只能做「選擇」而不能做「計算」或「編造」。

---

## 5. 三份資料副本與一致性要求

系統中同一組數字實際上會存在**三份副本**，設計上必須保證三者一致：

| 副本 | 位置 | 用途 | 產生方式 |
|------|------|------|----------|
| ① Chart XML 快取值 | `.pptx` 內 `chart#.xml` | PPT 畫面顯示 | `add_chart()` 自動產生 |
| ② 內嵌 workbook | `.pptx` 內 `embeddings/*.xlsx` | 右鍵「編輯資料」開啟 | `add_chart()` 自動產生，與①同源 |
| ③ FR-3 外部稽核 `.xlsx` | 獨立檔案，跟 PPT 一起寄送 | 使用者/主管獨立核對、稽核 | `excel_exporter.py` 從**相同**的 ChartSpec / MetricStore 產生 |

①②由同一次 `add_chart()` 呼叫產生，保證一致；③是另一條獨立輸出路徑，但輸入源頭相同（都來自 MetricStore → ChartSpec），數值仍然一致。三者不會互相「連動」——即使使用者事後在 PPT 裡雙擊改了內嵌工作表的值，也不會回寫影響外部稽核 `.xlsx`，這是預期行為。

---

## 6. 模組佈局與實作狀態

所有簡報生成程式位於 `src/ppt_generation/`，依管線階段分成子套件。
模組使用說明另見 [`src/ppt_generation/Guide.md`](../src/ppt_generation/Guide.md)。

套件根目錄只保留 `run_pipeline.py` 這個唯一入口，其餘實作一律歸屬子套件。

```
ppt_generation/
├── Guide.md               模組使用說明
├── run_pipeline.py        端到端 CLI（支援 --stage 分階段停點）
├── core/                  跨階段共用：config / llm_client / placeholders
├── data/                  Stage 1-3
├── charts/                圖表定義與防呆
├── agents/                Stage 4
├── output/                Stage 5-6
└── verification/          Stage 7
```

| 檔案 | 功能 | 狀態 |
|------|------|------|
| `core/config.py` | 路徑常數、`load_credential()`（環境變數優先，回退 `.venv/api_key/`）、`LLMSettings` / `load_llm_settings()` | 已實作並驗證 |
| `src/ppt_generation/core/llm_client.py` | `complete_json()` / `complete_tool_call()` 統一介面；輕量 schema 驗證；OpenAI 相容與 Bedrock 兩條後端；指數退避重試 | 已實作並驗證 |
| `core/placeholders.py` | 佔位符語法解析與查表代入、裸數字偵測、`format_value()` | 已實作並驗證 |
| `data/dataset_loader.py` | 讀取 backend `UnifiedIngestionResult` JSON → pandas DataFrame，保留每格 evidence；排除低信度與未經人工確認的資料集 | 已實作並驗證 |
| `data/metric_engine.py` | 確定性指標計算（`.value` / `.period_growth` / `.yoy` / `.share` / `.rank` / `.forecast`）＋ `detect_axis_kind()` 防呆 | 已實作並驗證 |
| `data/metric_store.py` | `MetricSeries` / `MetricStore`：`catalog_for_llm()`（無數值）、`get()`、`computable_metric_keys()`、`blocked_metrics()`、JSON 持久化 | 已實作並驗證 |
| `charts/chart_builder.py` | `ChartSpec` / `ScatterSpec`；`add_category_chart()` / `add_pie_chart()` / `add_scatter_chart()` 統一走 `add_chart()`；`CHART_SKILLS` registry、`CHART_TYPE_BY_SKILL`、`CHART_SKILL_TOOL_SCHEMAS`、`dispatch_chart_skill()` | 已實作並驗證 |
| `charts/chart_planner.py` | `ChartPlan`、`validate_chart_plan()`、`resolve_chart_plan()`、`resolve_all()` | 已實作並驗證 |
| `agents/section_planner.py` | 章節規劃 + `NEEDS_CONFIRMATION` 中斷 + metric_key 白名單清理 | 已實作並驗證 |
| `agents/chart_agent.py` | tool use 取得 ChartPlan，驗證失敗回饋錯誤讓 LLM 自我修正 | 已實作並驗證 |
| `agents/narrative_writer.py` | `PageNarrative`（僅佔位符）+ 規則層自我校正迴圈 | 已實作並驗證 |
| `agents/reviewer.py` | 規則層（裸數字／引用一致／方向矛盾）+ LLM 語意層 | 已實作並驗證 |
| `output/renderer.py` | 套模板組頁（圖左文右）、經 CHART_SKILLS 插入原生圖表、代入敘事佔位符 | 已實作並驗證 |
| `output/excel_exporter.py` | FR-3 外部稽核 `.xlsx`，工作表命名 `P.{頁碼}_{指標名稱}`，含來源儲存格欄與索引頁 | 已實作並驗證 |
| `verification/verify_chart_consistency.py` | 三方比對（chart 快取 ↔ 內嵌 workbook ↔ 外部 `.xlsx`），CLI 以 exit code 回報 | 已實作並驗證 |
| `charts/table_builder.py` | `TableSpec`（繼承 `ChartSpec`）；`add_native_table()` / `add_heatmap_table()`；`format_value()` / `parse_value()` 成對格式化；`TABLE_SKILLS`、`TABLE_SKILL_TOOL_SCHEMAS` | 已實作並驗證 |
| `run_pipeline.py` | 串接全流程、處理 `NEEDS_CONFIRMATION` 中斷與退件重試；唯一 CLI 入口 | 已實作並驗證 |
| `src/core/generation_orchestrator.py` | backend 唯一可呼叫邊界 `generate_deck()`；吃 `GenerationRequest` 回 `GenerationResult` | 已實作並驗證 |

> v0.2 文件曾將 `metric_store.py` / `chart_planner.py` / `verify_llm_chart_flow.py` 列為已完成，實際檔案不存在。v0.3 已重新實作前兩者；`verify_llm_chart_flow.py` 的職責由端到端驗證取代，不再單獨建立。

### 6.2 端到端驗證結果

以模擬的 backend JSON（12 個月市場流通卡數 + 5 家銀行卡數）跑完整管線，
四個 Agent 的 LLM 呼叫以假回應注入（不需連網）：

```
Stage 1-3  資料集 2 個 → 可用指標 6 個，防呆擋下 4 個
Stage 4    章節規劃 READY(3 章) → 圖表 3 張(line/column/pie) → 敘事 3 頁 → 審查全數 APPROVED
Stage 5-6  outputs/deck.pptx（3 頁 3 圖表）+ outputs/deck_data.xlsx（索引 + 3 工作表）
Stage 7    T1 三方比對：3 個系列全部 PASS，且全部含外部 Excel 比對
交叉驗證   PPT 圖表顯示值 == 原始 backend JSON 數值（逐項相等）
```

負向測試：竄改稽核 Excel 中一個數字（21.4 → 99.9）後重跑驗證，
確實回報 FAIL 並以 exit code 1 結束，證明驗證器不是恆真。

### 6.3 已知落差

- **敘事平行化未實作**：`write_narratives()` 目前為序列執行，尚未依
  `LLM_MAX_PARALLEL` 平行化。敘事是整條管線最耗時的部分，正式跑大量頁面
  前需補上。
- **真實 LLM 尚未串接**：所有 Agent 的契約與防呆都已驗證，但驗證時用的是
  注入的假回應。真實模型是否能穩定產出合規的佔位符敘事（不寫裸數字），
  必須實際接上 API 後才能確認。
- **`.venv` 尚未安裝依賴**：驗證是借用系統 Python 環境完成的，
  專案虛擬環境目前只有 pip。

### 6.1 LLM 憑證管理約定

API 金鑰放在 `.venv/api_key/` 目錄下（`.venv` 已被 `.gitignore` 排除，不會進版控）：

```
.venv/api_key/key.txt        # 單一金鑰，或 KEY=VALUE 逐行格式
```

`config.py` 的載入規則：

1. 優先讀環境變數（`LLM_API_KEY` / `OPENAI_API_KEY` / `AWS_*`），CI 與容器部署走這條路
2. 環境變數不存在時，回退讀取 `.venv/api_key/key.txt`
3. 檔案內容支援兩種格式：整檔即金鑰（去除空白）、或 `KEY=VALUE` 逐行（可放多組後端憑證）
4. 金鑰值**永不寫入日誌、錯誤訊息或產出檔案**；錯誤訊息只提示 key 名稱

> 生產部署不應把憑證放在 `.venv` 內（虛擬環境可能被重建）。此約定僅適用本機開發與競賽 Demo；正式環境應改用環境變數或 AWS Secrets Manager。

---

## 7. 已驗證的 POC 成果

### 7.1 驗證邏輯

1. 從 `chart.plots[0].series[0].values` 讀出畫面顯示值（chart XML cache）
2. 從 `chart.part.chart_workbook.xlsx_part.blob` 讀出內嵌工作表的二進位內容，用 openpyxl 解析出實際數值
3. 比對兩者是否完全相等

### 7.2 驗證結果

```
[Slide 4] 圖表: 市場流通卡數（萬張）
  畫面顯示值 : [5865.5, 5874.8, ..., 6048.6]
  內嵌工作表值: [5865.5, 5874.8, ..., 6048.6]
  一致性: PASS

=== 最終結果: 全部一致 (PASS) ===
```

證實：套用模板生成的圖表，右鍵「編輯資料」看到的內嵌工作表數字，與畫面顯示完全一致。

---

## 8. 圖表類型對應與已知限制

| 附件二要求的圖表 | python-pptx 對應方案 | 狀態 |
|---|---|---|
| 排名圖、成長率圖（長條/橫條） | `CategoryChartData` + `COLUMN_CLUSTERED` / `BAR_CLUSTERED` | 已實作並驗證 |
| 市占率圖（圓餅圖） | `CategoryChartData` + `PIE` | 已實作（`add_pie_chart()`），限制單一系列 |
| 散點圖（規模 vs 成長） | `XyChartData` | 已實作 `add_scatter_chart()`，**資料點標籤（銀行名稱）尚未支援** |
| 熱力圖 | 無原生圖表類型 | 已實作 `table_builder.add_heatmap_table()`：原生表格 + 資料驅動色階。**不屬於「圖表」，右鍵無「編輯資料」選項** |
| 一般原生表格 | `shapes.add_table()` | 已實作 `table_builder.add_native_table()`（FR-2.4） |
| 雙軸圖（如流通卡數＋簽帳金額疊圖） | 無高階 API | 已實作 `add_combo_chart()`：先 `add_chart()` 寫入全部系列，再把後段 `<c:ser>` 搬到新建的 `<c:lineChart>` 並掛次軸 |

### 8.1 雙軸圖的做法與為什麼只有這一條路

附件三 P.5 是流通卡數（長條）+ 簽帳金額（折線）的雙軸疊圖，兩者量級差一個
數量級。python-pptx 沒有雙軸的高階 API，可行做法有兩條，但只有一條是對的：

- ✗ 自己組 chart XML 並手填 `<c:numCache>`：快取值會有，但**內嵌 workbook
  不會有**。使用者右鍵「編輯資料」看到空表或舊值——這正是附件三揭露的病灶。
- ✓ 先用 `add_chart()` 一次把**所有**系列（含之後要變折線的）寫進去，讓
  python-pptx 把內嵌 workbook 寫完整；再把後段 `<c:ser>` 節點從 `<c:barChart>`
  搬到新建的 `<c:lineChart>`，並補一組次軸（`catAx` 設 `delete=1`、`valAx`
  設 `axPos=r`）。

搬動的是「這個系列用什麼圖形畫」，全程沒有觸碰任何數值節點，所以三份副本的
一致性仍由那一次 `add_chart()` 保證。實作細節上有兩個會讓 PowerPoint 判定
檔案損毀的坑：次軸的 `axId` 不得與主軸相撞；`invertIfNegative` 是長條專屬
子元素，留在折線 `ser` 裡會違反 schema。兩者都有測試守著
（`tests/test_combo_chart.py`）。

一個連帶修正：`verify_chart_consistency` 原本只檢查 `chart.plots[0]`，
雙軸圖是兩個 plot（`barChart` + `lineChart`），不改的話次軸那組系列會**靜默
漏驗**——而次軸系列正是最需要核對的那組。

### 8.2 原生表格的數字怎麼守

表格走 `add_table()`，沒有內嵌 workbook，右鍵也沒有「編輯資料」。這不是缺陷，
是 PowerPoint 表格的本性，但後果是表格數字少一份可互相印證的副本。因此：

- 儲存格文字**只能**由 `table_builder.format_value()` 產生（唯一格式化入口）
- 驗證時用 `parse_value()` 反向解析，與稽核 Excel 比對；比對前把稽核值套用
  同一套格式化規則，避免被顯示精度的四捨五入誤判
- **沒有稽核 Excel 的表格一律視為「無法驗證」，不是「通過」**

### 8.3 待解決事項

- **散點圖標籤**：附件三 P.10 每個資料點旁需顯示銀行名稱（中信、玉山...），python-pptx 無高階 API，需直接操作 `c:dLbls` XML 節點。
- **雙軸圖的 PowerPoint 實機驗收**：XML 結構、軸配對與內嵌工作簿一致性已有自動化測試，但「右鍵編輯資料」尚未在 PowerPoint 實機開啟確認。
- **熱力圖是否符合「圖表可獨立修改」要求**：目前走表格模擬（規格書 FR-2.3 明訂的替代方案），需與命題要求的「圖表」定義做確認。
- **章節規劃 Agent 的中斷機制**：`NEEDS_CONFIRMATION` 需對應 API 層非同步 job 狀態機（如 `AWAITING_USER_INPUT`），目前僅有設計。
- **低信度資料的阻擋策略**：backend 回傳 `requires_human_review=True` 的資料集，是否直接拒絕進入 MetricStore，或標記後允許但在簡報上加註，尚未定案。

---

## 9. 與模板（`source/template.pptx`）的整合方式

### 9.1 模板現況

- 5 頁骨架，6 種版面配置（標題頁 / 章節標題頁 / 標題及內容頁等）
- 目前**沒有任何圖表物件**，僅有標題、內容文字框（BODY placeholder）、頁碼 placeholder

### 9.2 生成邏輯

因為模板只是骨架，生成流程是「新增內容」而非「修改既有圖表」：

```
1. 章節規劃 Agent 決定本頁對應哪個 SectionPlan（如「市場整體概況」）
2. Renderer 選擇對應版面（如「1_標題及內容」），填入章節標題
3. 圖表 Agent 已產出的 ChartSpec，依 chart_type 呼叫 CHART_SKILLS[chart_type](...)
   插入圖表到指定座標（left/top/width/height）
4. 文字生成 Agent 產出的 PageNarrative，經佔位符代入實際數值後填入內容文字框
5. 若該頁需要表格（如熱力圖模擬），另外用 python-pptx 原生 table API 插入
```

POC 已示範套用第 4 頁（`1_標題及內容` 版面）插入圖表並驗證通過；文字生成／審查 Agent 的頁面組裝流程尚未實作。

### 9.3 視覺風格：對齊附件三的版面語彙

模板提供的是骨架（版面、底圖、頁碼），**版面語彙**則來自
`source/附件三_信用卡範例簡報及錯誤說明.pptx`。實測解析該檔得到：

| 元素 | 附件三的做法 |
|---|---|
| 標題 | 左上、22.5pt、深灰 `1A1A1A`、微軟正黑體 |
| 重點訊息帶 | 標題下方一條全寬色塊，內含一行「◆ 結論句」，粗體 |
| 主體文字 | 13.5pt / 10.5pt 兩級，中灰 `4D4D4D`，標籤 `666666` |
| 品牌強調 | 台新紅 `C12026`（全篇 89 處） |
| 章節頁 | `CHAPTER 0N`（紅）+ 章節名（40pt 粗體灰）+ 紅色底線 |
| 圖表頁註記 | 頁尾「可點選圖表右鍵『編輯資料』查看原始 EXCEL 資料表」 |

這些常數集中在 `src/ppt_generation/core/theme.py`，是唯一來源。放在 `core/`
而非 `output/`，是因為 `charts/` 也要用它為圖表上色；留在 `output/` 會讓
`charts` 的匯入繞經 `output/__init__` → `renderer` → `charts` 形成循環匯入。

**圖表配色：白 → 台新紅單色階，不用模板預設調色盤。** 不設系列顏色的話，
圖表會繼承模板主題的 `accent1..accent6`，一張圖出現藍、橘、灰、黃四種不相干
的顏色——顏色沒有承載任何資訊，又與台新紅的品牌語彙互相打架。規則是：

| 情境 | 配色 |
|---|---|
| 單系列（排名圖、市占率圓餅圖） | 逐**資料點**依大小排名取 `FBE4E5` → `C12026` 漸層，深紅即最大值 |
| 多系列（雙軸圖、多指標走勢） | 系列間紅／黑交錯：`C12026` → `262626` → `E27A7E` → `8C8C8C` |
| 熱力圖儲存格 | `FFFFFF` → `C12026`，與圖表共用同一條「越深越大」規則 |
| 格線、框線 | `D9D9D9`，淺到只夠界定範圍 |

漸層低端刻意不取純白：投影片底色就是白色，純白的長條等於沒有長條。

實作在 `chart_builder.apply_chart_style()`，是所有圖表的唯一樣式入口。
它只寫 `c:spPr`（形狀屬性）與 `c:txPr`（文字屬性），**完全不觸碰
`c:numCache` / `c:strCache` 等數值節點**，因此 §2.2 的三份副本一致性
與上色無關——那是 `add_chart()` 一次寫好的。T1 在上色後仍 22/22 通過。

**字級是算出來的，不是常數。** 同一個文字框在不同頁面要裝的字數差三倍，
固定字級的結果就是「短的看起來空、長的擠到溢出」。`theme.fit_font_size()`
與 `fit_single_line_font_size()` 依文字長度與框寬高推算字級，夾在該用途的
上下限之間；PowerPoint 的 `normAutofit` 只留作最後保險——它只會縮小不會
放大，短敘事靠它永遠補不回來。圖表內的字級另有一個坑：python-pptx 建立的
`chartSpace` 帶著 `sz="1800"`，任何沒被逐一設定的元素都會以 18pt 渲染，
在 60% 寬的圖表區裡直接糊成一團，因此 `apply_chart_style()` 會先把圖表層級
的預設字級收到 9pt。

**黃色的用途。** 附件三本身用黃底 `FFFF00` 標示「要讀者特別注意的地方」
（命題方的錯誤批註與那句右鍵提示）。本專案沿用這個語彙，但指向真正的重點：

1. **重點訊息帶**：淡黃底 `FFF8D6` + 左緣純黃標示條 `FFC000`，放該頁結論句
2. **敘事中的數字**：凡由 MetricStore 代入的數值，一律加 `a:highlight` 黃色標示

第二項讓「哪些字是系統算的」在簡報上肉眼可辨，是 FR-3.4 溯源的第一層線索，
也讓漏網的裸數字（沒被標黃卻是數字）一眼可見。實作上 `render_segments()`
與 `render_text()` 共用同一組查表函式，串接結果逐字相同，不會出現第二種
代入結果。台新紅保留給品牌與結構性強調，與黃色分工。

---

## 10. 下一步規劃

1. ~~**串接真實 LLM API**~~：**已完成**（2026-07-30，Gemini `gemini-3.5-flash-lite`
   / `gemini-3.1-flash-lite`，8 頁全數一次過規則檢查，T1 71/71 PASS）。
   首跑抓到三個假 LLM 永遠測不到的契約問題：schema 沒送進 prompt 導致模型
   把 `sections` 猜成 `pages`、非必填欄位的 null 被當型別錯誤、429 限流沒有
   重試。細節與對策見 `src/ppt_generation/Guide.md`
2. ~~**`orchestrator.py`**~~：**已完成**，即 `src/core/generation_orchestrator.py`
   的 `generate_deck()`，是 backend 唯一可呼叫邊界；`NEEDS_CONFIRMATION` 中斷改由
   backend 的 `waiting_review` job 狀態與人工複核端點處理
3. ~~**把驗證改寫為 pytest 測試套件**~~：**已完成**，測試在 repo 根的 `tests/`
   與 `src/backend/tests/`，合併關卡為 `scripts/verify_all.py`
4. ~~**擴充 `chart_builder.py`**~~：**已完成**雙軸圖（`add_combo_chart()`）；
   散點資料點標籤仍待處理（需操作 `c:dLbls`）
5. ~~**`table_builder.py`**~~：**已完成**熱力圖模擬與一般原生表格
6. ~~**安裝依賴到 `.venv`**~~：**已完成**；版本鎖定見 `requirements.lock`
7. **敘事平行化**：依 `LLM_MAX_PARALLEL` 平行執行 `write_narratives()`
8. **DeckSpec Refresh**：`deckspec.json` 已隨每次生成輸出，
   但 `replay(deckspec, new_data)` 一鍵重生成尚未實作（規格書 A2）

> 註：`charts/table_builder.py` 之所以歸在 `charts/` 而非 `output/`，是因為熱力圖
> 模擬需要定義「資料 → 表格樣式」的對應規則，性質與 chart_builder 相同；
> 實際插入投影片的動作仍由 `output/renderer.py` 負責。
